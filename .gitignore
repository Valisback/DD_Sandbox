# Terraform
*.tfstate
*.tfstate.*
terraform.tfplan
terraform.tfplan.*
.terraform/
.terraform.lock.hcl
terraform.tfvars

# Datadog configuration (contains secrets)
configs/config.yaml
configs/values.yaml

# Kubernetes secrets (contains API keys)
kubernetes/datadog/datadog-secret.yaml

# OS
.DS_Store
Thumbs.db 